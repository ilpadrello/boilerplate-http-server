The content of this folder is not versioned!
The intention of this folder is to change configuration files without change the versioned versions.
The config inside this folder must have the same name as the one you want to override in the parent folder.
You don't need to rewrite all the config file, only the parts you want to override, but conflicts objects do not merge.
